"use client";

/**
 * Quran Amiri / Scheherazade are declared + preloaded from the document layout.
 * Kept as a no-op so existing imports do not break.
 */
export default function QuranFontLoader() {
  return null;
}
